#Tuples similar to list but can't be modified
numbers = (1, 2, 3)
#numbers[0]=10 we can't change them at all
print(numbers[0])
print(numbers.count('1'))#to get count
print(numbers.index(3))#to get element index